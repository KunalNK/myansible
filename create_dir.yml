---
    
    
    - name: run tasks on all hosts
      hosts: "*"
      tasks:
           
        - name: create_directory
          file:
            path: $HOME/test_directory
            state: directory
         
        